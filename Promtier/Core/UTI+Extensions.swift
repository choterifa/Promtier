import UniformTypeIdentifiers

extension UTType {
    static let promtierPromptId = UTType("com.promtier.prompt.id") ?? .plainText
    static let promtierFolderId = UTType("com.promtier.folder.id") ?? .plainText
}
